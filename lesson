명령어 정리
mkdir (이름) 디렉토리 생성
rm (이름) 파일 제거, 디렉토리의 경우 -rf 를 붙일것
cd (경로) 현재 위치 이동
cd .. 상위디렉토리로 이동
vim (이름) vim 편집기로 파일 생성
gcc (이름1) -o (이름2)  '이름1' 파일을 컴파일하여 '이름2'라는 파일 생성
ls 현재 위치에 있는 모든 파일 보기
ls -al 현재 위치에 있는 모든 파일(숨겨진것 포함) 및 권한,크기,날짜 보기
wpd 현재 위치 확인하기
./(이름) 컴파일한 '이름'파일 실행하기 여기서 . 은 현재 파일 위치를 말함
git add . 현재 위치에 있는 파일을 모두 깃허브에 추가함
git commit -m "(파일이름) upload" 잘 기억안남
git push 'git add' 로 추가한 파일들을 깃허브에 업로드함


vim 명령어
:w 현재까지 vim편집기로 작성한 내용 저장
:q vim편집기에서 나가기
:wq 작성한 내용을 저장하고 나가기
